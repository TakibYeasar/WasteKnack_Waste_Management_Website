const PickupStatusTracking = () => (
    <div>
        <h2 className="text-xl font-bold mb-4">Pickup Status Tracking</h2>
        <p>Check the status of pickup requests (pending, assigned, completed).</p>
    </div>
);

export default PickupStatusTracking;
