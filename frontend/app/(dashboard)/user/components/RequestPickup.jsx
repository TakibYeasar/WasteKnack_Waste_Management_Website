const RequestPickup = () => (
    <div>
        <h2 className="text-xl font-bold mb-4">Request Pickup</h2>
        <p>Submit a pickup request specifying waste type, schedule pickup time, and optionally upload photos.</p>
    </div>
);

export default RequestPickup;
